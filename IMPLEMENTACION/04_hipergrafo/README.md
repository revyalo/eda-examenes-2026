# Implementacion: hipergrafo

Proyecto de implementacion consolidado desde el material PDF original (`03_impl_hipergrafo`).
Contiene entorno, clases de examen y tests. Completa solo los `TODO`.

Ejecuta:

```bash
mvn test
```
