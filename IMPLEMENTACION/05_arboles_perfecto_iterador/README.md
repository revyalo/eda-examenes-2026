# Implementacion: arbol perfecto e iterador

Proyecto de implementacion consolidado desde el material PDF original (`08_impl_funcionalidades_arboles_perfecto_iterador`).
Contiene entorno, clases de examen y tests. Completa solo los `TODO`.

Ejecuta:

```bash
mvn test
```
