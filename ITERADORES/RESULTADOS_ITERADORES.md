# Resultados de iteradores

Tras la unificacion, los proyectos se han comprobado con compilacion de tests:

```bash
mvn test -DskipTests
```

Resultado: `ITERADORES/arboles`, `ITERADORES/arboles_binarios` e `ITERADORES/grafos` compilan.

No se ejecuta `mvn test` como validacion final porque los ejercicios estan deliberadamente en esqueleto y deben fallar hasta que completes los TODOs.
